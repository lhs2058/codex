import type { ImportParseResult, WorkbookSheet } from "../contracts";
import { normalizeLineName, normalizeProductionDate, normalizeQuantity } from "../normalize";
const slots=["A","B","C","D","E"] as const;
export function parseProductionWorkbook(sheets:WorkbookSheet[]):ImportParseResult{const rows:ImportParseResult["rows"]=[],diagnostics:ImportParseResult["diagnostics"]=[];for(const s of sheets.filter(x=>/^\d{2}\.\d{2}$/.test(x.sheet))){const title=s.data[1]?.find((cell)=>typeof cell==="string"&&/báo cáo sản lượng.*ngày/i.test(cell));const date=typeof title==="string"?title.match(/(\d{2})\/(\d{2})\/(\d{4})/):null;const grouped=s.data[2]?.some((cell)=>typeof cell==="string"&&/sản lượng từng time/i.test(cell));const slotsHeader=s.data[3]?.filter((cell)=>typeof cell==="string"&&/^time [a-e]/i.test(cell)).length??0;if(!date||!grouped||slotsHeader<5){diagnostics.push({sourceSheet:s.sheet,sourceRow:1,code:"missing-required-value",message:"Production grouped signature missing",field:"headers"});continue;}for(let i=6;i<s.data.length;i++){const r=s.data[i]??[],line=r[4],model=r[5];if(!line||!model||/total/i.test(String(line))){if(r.some(Boolean))diagnostics.push({sourceSheet:s.sheet,sourceRow:i+1,code:"missing-required-value",message:"Production row lacks a model or line",field:!model?"modelCode":"lineCode"});continue;}const lineText=String(line);let process:"AOI"|"ROUTER"|"XRAY"|"ICT"="AOI";/* Line 1–4 are the main-line AOI-completion output; named external stages map explicitly. */if(/xray/i.test(lineText))process="XRAY";else if(/ict/i.test(lineText))process="ICT";else if(/router/i.test(lineText))process="ROUTER";for(let j=0;j<5;j++){const actual=r[14+j*5],down=r[16+j*5],note=r[17+j*5];if(actual==null){diagnostics.push({sourceSheet:s.sheet,sourceRow:i+1,code:"missing-required-value",message:"Time slot has no actual quantity",field:"actualQty"});continue;}try{const lineCode=process==="ICT"?"ICT-1":/router may 2/i.test(lineText.normalize("NFD").replace(/[\u0300-\u036f]/g,""))?"ROUTER-2":normalizeLineName(line);rows.push({sourceSheet:s.sheet,sourceRow:i+1,productionDate:`${date[3]}-${date[2]}-${date[1]}`,shiftCode:String(r[3]??"DAY"),timeSlotCode:slots[j],lineCode,modelCode:String(model),processCode:process,inputQty:0,actualQty:normalizeQuantity(actual,"actualQty"),okQty:0,ngQty:0,downtimeMinutes:down==null?0:normalizeQuantity(down,"downtimeMinutes"),downtimeReasonCode:null,note:typeof note==="string"?note:""});}catch(error){diagnostics.push({sourceSheet:s.sheet,sourceRow:i+1,code:"invalid-count",message:String(error),field:"actualQty"});}}}}return {kind:"production",rows,diagnostics};}
