import { useEffect,useRef,useState } from "react";
import { createMasterDataRepository,type MasterDataRepository } from "../../data/repositories/master-data-repository";
import type { MasterDataSnapshot } from "../../domain/types";
import { MasterDataEditor } from "./MasterDataEditor"; import { StandardTimeEditor } from "./StandardTimeEditor"; import { UserEditor,type NewUser } from "./UserEditor";
async function invokeAdminCreateUser(input:NewUser){const client:any=(await import("../../data/supabase")).getSupabaseClient();const {error}=await client.functions.invoke("admin-create-user",{body:input});if(error)throw Error(error.message||"user_creation_failed");}
export function AdminPage({repository,createUser=invokeAdminCreateUser}:{repository?:MasterDataRepository;createUser?:(input:NewUser)=>Promise<unknown>}){const[snapshot,setSnapshot]=useState<MasterDataSnapshot|null>(null);const[error,setError]=useState("");const[mutating,setMutating]=useState(false);const mounted=useRef(true);const generation=useRef(0);const lock=useRef(false);const repo=()=>repository??createMasterDataRepository();const refresh=async()=>{const current=++generation.current;try{const value=await repo().listMasterData();if(mounted.current&&current===generation.current){setSnapshot(value);setError("");}}catch(e){if(mounted.current&&current===generation.current)setError(e instanceof Error?e.message:"Unable to load master data.");}};const mutate=async(action:()=>Promise<unknown>)=>{if(lock.current)return;lock.current=true;setMutating(true);setError("");try{await action();await refresh();}catch(e){if(mounted.current)setError(e instanceof Error?e.message:"Unable to save changes.");throw e;}finally{lock.current=false;if(mounted.current)setMutating(false);}};useEffect(()=>{mounted.current=true;void refresh();return()=>{mounted.current=false;generation.current++;};},[repository]);return <main><h1>Administration</h1><p>Admin workspace</p>{!snapshot&&!error&&<p role="status">Loading master data…</p>}{error&&<p role="alert">{error}</p>}{snapshot&&<><MasterDataEditor disabled={mutating} snapshot={snapshot} createModel={x=>mutate(()=>repo().createModel(x))} deactivateDowntimeReason={(id,v)=>mutate(()=>repo().deactivateDowntimeReason(id,v))}/><StandardTimeEditor disabled={mutating} snapshot={snapshot} save={x=>mutate(()=>repo().saveStandardTime(x))}/><UserEditor disabled={mutating} createUser={x=>mutate(()=>createUser(x))}/></>}</main>;}
